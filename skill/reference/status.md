# status

```bash
node <bridge-skill>/scripts/status.js [--json]
```

Reports PRODUCT/CONTRACT/ARCHITECTURE/DESIGN, Keel·Impeccable install paths, optional `keel status`, and `.bridge/plan.md` progress.

Playbook: if scripts missing, do the same checks manually — invent nothing.
