# Next commands (Bridge)

1. Candidates = **pending gaps from this run** (CONTRACT open questions, sync gaps, ship checklist fails, unfinished plan steps).
2. Emit only if non-empty; else quiet / `No pending issues — no next commands.`
3. Max **2–3**.
4. **Only `/bridge …` commands.** Never suggest `/keel` or `/impeccable` (or any other skill) to the user.
5. Cite the gap in the user’s language, e.g.  
   `1. `/bridge build be:clarify` — fecha gap: error envelope ≠ CONTRACT`  
   `2. `/bridge sync` — revalidar após o build`
6. Do not auto-run the suggested next Bridge command unless the user asked to continue.
