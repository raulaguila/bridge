# Next commands (Bridge)

1. Candidates = **pending gaps from this run** (CONTRACT open questions, sync gaps, ship checklist fails).
2. Emit only if non-empty; else quiet / `No pending issues — no next commands.`
3. Max **2–3**. Prefer Bridge commands for contract/sequence; Keel/Impeccable when craft must close the gap.
4. Cite the gap in the user’s language:
   `1. `/keel clarify api` — fecha gap: error envelope ≠ CONTRACT`
5. Never auto-run.
