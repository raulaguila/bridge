# doctor

Validate Bridge artifacts (PRODUCT/CONTRACT stamps, status, skills).

```bash
node <bridge-skill>/scripts/doctor.js [--json] [--fix]
```

`--fix` only auto-adds schema stamps. Empty accepted APIs = fail.

Also see [status.md](status.md) for the pulse CLI.
