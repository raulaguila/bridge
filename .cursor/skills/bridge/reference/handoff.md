# handoff

**Deprecated alias of [build.md](build.md).**

`/bridge handoff` behaves exactly like `/bridge build`: run craft via Keel/Impeccable **internally**.  
Do **not** emit copy-paste `/keel` / `/impeccable` lines for the user.
